{
  "name": "globmint-contracts",
  "version": "0.1.0",
  "description": "Globmint Vault smart contract — non-custodial per-user stablecoin savings",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
